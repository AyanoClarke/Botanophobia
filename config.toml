# The URL the site will be built for
base_url = "https://botanophobia.xyz"

# Used in RSS by default
title = "Botanophobia"
description = ""
# The default language, used in RSS
default_language = "en"

# Theme name to use
# theme = "dinkleberg"

# Highlight all code blocks found
highlight_code = true

# Which theme to use for the code highlighting.
# See below for list of accepted values
highlight_theme = "cheerfully-light"

# Whether to generate a RSS feed automatically
generate_rss = true

# The number of articles to include in the RSS feed. Will include all items if
# not set (the default).
# rss_limit = 20

# Whether to copy or hardlink files in static/ directory. Useful for sites
# whose static files are large. Note that for this to work, both static/ and
# output directory need to be on the same filesystem. Also, theme's static/
# files are always copies, regardles of this setting. False by default.
# hard_link_static = false

# The taxonomies to be rendered for that site and their configuration
# Example:
#     taxonomies = [
#       {name = "tags", rss = true}, # each tag will have its own RSS feed
#       {name = "tags", lang = "fr"}, # you can have taxonomies with the same name in multiple languages
#       {name = "categories", paginate_by = 5},  # 5 items per page for a term
#       {name = "authors"}, # Basic definition: no RSS or pagination
#     ]
#
taxonomies = [
 {name = "categories"},
 {name = "tags"},
]

# The additional languages for that site
# Example:
#     languages = [
#       {code = "fr", rss = true}, # there will be a RSS feed for French content
#       {code = "fr", search = true}, # there will be a Search Index for French content
#       {code = "it"}, # there won't be a RSS feed for Italian content
#     ]
#
languages = []

# Whether to compile the Sass files found in the `sass` directory
compile_sass = true

# Whether to build a search index out of the pages and section
# content for the `default_language`
build_search_index = false

# A list of glob patterns specifying asset files to ignore when
# processing the content directory.
# Defaults to none, which means all asset files are copied over to the public folder.
# Example:
#     ignored_content = ["*.{graphml,xlsx}", "temp.*"]
ignored_content = []

# A list of directories to search for additional `.sublime-syntax` files in.
extra_syntaxes = []

# Optional translation object. The key if present should be a language code.
# Example:
#     default_language = "fr"
#
#     [translations]
#     [translations.fr]
#     title = "Un titre"
#
#     [translations.en]
#     title = "A title"
#
[translations]

# You can put any kind of data in there and it
# will be accessible in all templates
[extra]
# blog_logo="/imgs/common/logo.png" #will appear on top header
blog_title="Botanophobia::Logging" #will appear on top header after logo

katex_enable = true
katex_auto_render = true

## i18n words
label_tags = "标签"
label_tag = "标签"
label_categories = "类别"
label_category = "类别"
label_relative_posts = "相关文章"
label_next = "向后"
label_previous = "向前"
label_page = "页数"
label_of = " / "
label_minutes = "分钟"

og_image="" # Image that will appear on social media
og_alt_image="" # Alt for og_image
og_site_name="" # Site Name for Open Graphic
keywords="" # Keywords for SEO

educational_use="knowledge share" # OPTIONAL
copyright_year="2019" # OPTIONAL

fb_app_id="???" # OPTIONAL, Facebook App Id to help in metrics
twitter_username="@clarkedlh" # OPTIONAL, Twitter User to help with metrics

## Sidebar automatic links
sidebar = [
    {name = "Social", urls=[
        {name="Telegram", url="https://t.me/botanophobia"},
        {name="Twitter", url="https://twitter.com/ClarkeDLH"},
        {name="Github", url="https://github.com/AyanoClarke"},
    ]},
    {name = "Tools", urls= [
        {name="Platform 9 3'4", url="http://home.ustc.edu.cn/~gjzkk2"},
        {name="RSS", url="/rss.xml"},
    ]},
]

